{
  "name": "org-iso-15118",
  "private": true,
  "description": "ISO 15118 V2GTP (V2G Transfer Protocol) transport framing in portable .cljc; EXI application payload treated as opaque bytes",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
